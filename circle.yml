machine:
  environment:
    PATH: $HOME/.cabal/bin/:$PATH
dependencies:
  override:
    - cabal update
    - cabal install shellcheck
test:
  override:
    - shellcheck --version
    - shellcheck --exclude=SC2016,SC2054,SC2191 ./*.sh

